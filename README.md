#Created the python application
expose the application over 8000 port 
#add the flask in reuqirement.txt
#created the docker file as multistage
#create the jenkins file with build,test,docker build and deploy stage
pending #Create the deployment and add healt check and deploy it 